/**
 * Constants for application
 */
export default class AppConstans {
    // Base host for application 
    static BASE_HOST = "http://localhost:8080";
    // Login url for acess
    static LOGIN_URL = "/token/generate-token";
}